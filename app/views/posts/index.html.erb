<div class="col-md-12">
  <div class="group">
</div>

  <table class="table table-hover">
    <thead>
      <tr>
        <td>#</td>
        <td>Post id</td>
        <td>belongs to</td>
        <td>Post content</td>
      </tr>
    </thead>
    <tbody>
      <% @posts.each do |post| %>
        <tr>
          <td>#</td>
          <td><%= post.id %></td>
          <td><%= post.group_title %></td>
          <td><%= post.content %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
